<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="chen.questiong">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".chapter.ChapterOne" />
        <activity android:name=".chapter.ChapterTow" />
        <activity android:name=".chapter.ChapterThree" />
        <activity android:name=".menu.Mark" />
        <activity android:name=".menu.Setting" />
        <activity android:name=".menu.Exercise" />
        <activity android:name=".menu.Introduce" />
        <activity android:name=".Wellcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".chapter.NewPage"></activity>
    </application>

</manifest>